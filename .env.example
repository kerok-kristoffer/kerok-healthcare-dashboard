# Copy to .env and fill in (or set via PyCharm Run Config > Environment)
AWS_DEFAULT_REGION=us-east-1
ATHENA_S3_OUTPUT=s3://<output-bucket>/
ATHENA_WORKGROUP=primary
ATHENA_DATABASE=<database-name>
ATHENA_CATALOG=AwsDataCatalog

# One of the following auth methods (profile OR keys) — recommend profile locally:
# AWS_PROFILE=default
# AWS_ACCESS_KEY_ID=...
# AWS_SECRET_ACCESS_KEY=...
# AWS_SESSION_TOKEN=...
